[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  CI = "false"

# Handle SPA routing - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
