# Use package-lock.json for exact versions
package-lock=true

# Legacy peer deps for compatibility
legacy-peer-deps=true

# Production only for Render
production=false

# Optional: Use specific registry
registry=https://registry.npmjs.org/
